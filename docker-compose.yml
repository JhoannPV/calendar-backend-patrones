services:
  calendar-api:
    image: mongo:latest
    environment:
      MONGO_INITDB_ROOT_USERNAME: mern_user
      MONGO_INITDB_ROOT_PASSWORD: 1234567
    volumes:
      - ./mongo:/data/db
    ports:
      - 27017:27017